version: '3.7'

services:

  flask:
    build: 
      context: flask
    restart: always
    ports:
      - "8080"

  nginx:
    build: 
      context: nginx
    container_name: nginx
    restart: always
    depends_on:
      - flask
    ports:
      - "80:80"
